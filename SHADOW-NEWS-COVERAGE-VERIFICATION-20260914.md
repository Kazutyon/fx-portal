# シャドー経済ニュース・カバレッジ検証（2026-09-14）

## 検証範囲と結論

この検証は、リポジトリ内に存在するシャドー運用記録、仕様、実装、テスト記録だけを対象とした。外部サイト、GitHub Actions、artifact、本番環境にはアクセスしていない。

総合判定は **保留（カバレッジ不適合、時刻変換はテスト適合、実発生との照合データ不足）** とする。

- 2026-08-03〜2026-08-14の10営業日はActions成功率10/10だったが、高重要度イベントの独立確認は1/19件（約5.3%）にとどまる。
- 18/19件は「見落とし」と断定できる記録ではなく、独立した公式ソースで確認できなかったイベントである。取得個票がないため、一次取得自体の成否は判別できない。
- FRED追加で雇用統計、CPI、PPI、JOLTSの公式確認範囲は拡張されたが、追加後の該当日におけるシャドー実測結果はローカル記録にない。
- JSTへの日付・時刻変換、時刻形式、対象日抽出、安全ゲートはテストに適合した。一方、予定時刻と実際のニュース発生・公表時刻を突合する記録はない。

## 重要ニュースの捕捉判定

**判定: 不適合。取得処理の稼働は安定しているが、重要ニュースを十分に確認できたとは判定できない。**

根拠は次のとおり。

1. `CURRENT.md` と `LOG.md` は、2026-08-03〜2026-08-14の10営業日について、Actions成功率10/10、高重要度確認率1/19件（約5.3%）、確認できたのは2026-08-04のBEA公式貿易収支のみと記録している。
2. したがってジョブの取得・保存処理は10日間動作したが、重要イベントの独立確認という目的に対する実測カバレッジは不足している。
3. `ECONOMIC-CALENDAR-SOURCE-AUDIT.md` には、既存ポータルの代表日比較で、2026-06-26の米卸売在庫・ミシガン大学消費者信頼感指数、2026-07-02の米貿易収支・要人発言等、2026-07-03の欧州PMI改定値・要人発言等の欠落が記録されている。これは既存生成工程に捕捉漏れがあった直接的な証拠である。
4. FRED追加後は米国の雇用統計、CPI、PPI、JOLTSを公式日程として確認できる設計になった。ただし `LOG.md` の最終実機記録は2026-08-17で、その日はFRED対象発表が0件だった。改善効果は「見込み」にとどまり、該当日での実測確認は未完了である。
5. 対象ソースは主に予定表であり、突発的な重要ニュース、予定変更、発表結果そのものの捕捉を検証する記録はない。

## 見落とし・重複判定

**判定: 見落としは確認済み。シャドー期間の個別見落とし件数と実データ上の重複有無は判定不能。重複排除ロジックは限定条件でテスト適合。**

- 見落としについては、`ECONOMIC-CALENDAR-SOURCE-AUDIT.md` の3代表日に複数の欠落が明記されている。また `LOG.md` は公開済み25営業日の確認で、掲載漏れ、重複、別日混入を単発ではない生成工程の問題と判断したと記録している。
- 10営業日のシャドー評価で確認率が1/19件だったことは確認不足を示すが、残り18件を取得漏れとは扱わない。Forex Factoryには存在して公式ソースで未確認だった可能性があり、個票なしでは区別できないためである。
- `economic_calendar_validate.py` は、JST時刻・国・正規化名が同じイベントを1件へ統合する。2ソースの同一イベントが1件に統合され、`sources` に両ソースが記録されるテストは成功した。
- 重複キーは時刻・国・正規化名の完全一致に依存する。時刻差、表記言語の違い、略称・改定値表現の違いがある重複を同一視する仕様やテストはない。
- ローカルには10営業日分のイベント個票、日別差分、重複件数がないため、実運用データについて「重複なし」とは判定できない。

## 発生時刻への適合判定

**判定: 予定時刻のJST正規化は適合。実際の発生・公表時刻への適合は判定不能。**

- 仕様は `time_jst` を `HH:MM` または `TBD` とし、対象日をJSTで一致させる。
- Forex Factoryの入力から対象JST日だけを抽出し、21:30を維持するテストが成功した。
- BEAの米東部時間08:30を、2026-07-30のJST 21:30へ変換するテストが成功した。
- FRED/BLS相当では、雇用統計の08:30 ETをJST 21:30、JOLTSの10:00 ETをJST 23:00へ変換するテストが成功した。`ZoneInfo` を使うため、実装上は夏時間を含むタイムゾーン変換を行う。
- FRED Release Dates APIは日付だけを返し、時刻はコードに固定された慣行値で補っている。予定時刻の根拠と実発表時刻を日別に突合したローカル記録はない。
- ワークフローの05:15 JSTは当日予定の取得時刻であり、ニュースの発生時刻ではない。実発生時刻、取得時刻、検知時刻の差分を測るデータもない。

## テスト結果

2026-09-14にローカルで次を実行した。Pythonのバイトコード書込みを無効にし、既存のローカルIANAタイムゾーンデータを `PYTHONTZPATH` で参照した。

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'
$env:PYTHONTZPATH='C:\Program Files\Git\mingw64\share\zoneinfo'
python -m unittest test_economic_calendar_validate.py test_economic_calendar_fred.py -v
```

結果: **10件実行、10件成功**。

検証できた範囲は、対象日抽出、BEA/FREDのJST変換、2ソース同一イベントの統合、単一ソース警告、未確認時の公開不可、無効時刻の拒否、FRED対象リリースの生成である。実シャドー個票の網羅率、表記ゆれを含む重複、実発生時刻との差は、この単体テストの対象外である。

## 参照資料

- `CURRENT.md`: 10営業日の成功率、高重要度確認率、FRED追加後の残課題
- `LOG.md`: シャドー運用開始、初回実行、10営業日評価、既存テスト記録、FRED追加記録
- `ECONOMIC-CALENDAR-JSON.md`: JSON仕様、公開可能判定、単一ソース・重要指標0件の警告条件
- `ECONOMIC-CALENDAR-SOURCE-AUDIT.md`: 代表日の欠落、ソース範囲、公開前ゲート
- `DECISIONS.md`: 生データ保存、日時・件数・タイムゾーン照合、差分検出の設計原則
- `.github/workflows/economic-calendar-shadow.yml`: 平日05:15 JST実行、3ソース入力、14日保持artifact
- `economic_calendar_validate.py`: 検証、統合、確認済み判定、安全ゲート
- `economic_calendar_forexfactory.py`: Forex Factory日付・時刻正規化
- `economic_calendar_bea.py`: BEA米東部時間からJSTへの変換
- `economic_calendar_fred.py`: FRED対象リリースと固定予定時刻のJST変換
- `test_economic_calendar_validate.py`, `test_economic_calendar_fred.py`: 今回実行した10件のテスト

## 不足データ

最終判定を完了するには、少なくとも次のローカル記録が必要である。

- 2026-08-03〜2026-08-14の各ソースJSONと日別 `validation.json`
- FRED追加後の雇用統計、CPI、PPI、JOLTS該当日の各ソースJSONと検証結果
- 各高重要度イベントについて、基準となる公式イベント一覧、予定時刻、実公表時刻、取得時刻、検知時刻
- 日別の期待件数、取得件数、見落とし件数、重複件数、別日混入件数
- 表記ゆれ・時刻差を含む重複判定の期待結果とテスト記録
- 予定変更、延期、時刻未定、突発ニュース、要人発言を含むケースの記録

現リポジトリには `shadow-output/`、日別シャドーJSON、日別検証JSONが存在しない。ワークフロー仕様ではartifact保持が14日であり、`LOG.md` の集計値だけではイベント単位の再検証はできない。
