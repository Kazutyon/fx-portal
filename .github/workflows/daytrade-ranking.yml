name: Update daytrade ranking

on:
  workflow_dispatch:
  schedule:
    # 04:30 JST on weekdays (19:30 UTC on the previous day).
    # GitHub-hosted schedules can be delayed, so keep a wide buffer before the 07:01 report.
    - cron: "30 19 * * 0-4"

permissions:
  contents: write

concurrency:
  group: daytrade-ranking
  cancel-in-progress: false

jobs:
  update-ranking:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v5
        with:
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Generate ranking
        run: python daytrade_ranking.py

      - name: Commit updated data
        run: |
          if git diff --quiet -- data/daytrade-ranking.json; then
            echo "Ranking data did not change"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/daytrade-ranking.json
          git commit -m "Update daytrade ranking"
          git push origin main
